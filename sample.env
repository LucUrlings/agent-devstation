# Copy to .env. Empty SDK values mean unselected.
SDK_PYTHON=3.13
SDK_NODE=22.x
SDK_DOTNET=
SDK_JAVA=
SDK_GO=
SDK_RUST=
VSCODE_EDITOR_ENABLED=false
EDITOR_PASSWORD=
# On Linux, set these to `id -u` and `id -g` when bind mounts need it.
DEV_UID=1000
DEV_GID=1000
